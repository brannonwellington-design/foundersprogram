/** Shared 12-column layout (px-4 / 16px gutters mobile, md:px-6 / gap-x-6). */

export const grid12 = "md:grid md:grid-cols-12 md:gap-x-6";

/** Column 1 — row indices, narrow markers. */
export const gridCol1 = "md:col-start-1 md:col-span-1";

/** Columns 1–6 — hero left stack (pill, title, lead, CTA). */
export const gridHeroLeft = "md:col-start-1 md:col-span-6";

/** Columns 2–5 — primary left content (matches Mentors intro). */
export const gridLeft = "md:col-start-2 md:col-span-4";

/** Columns 2–6 — wider left media blocks. */
export const gridLeftWide = "md:col-start-2 md:col-span-5";

/** Columns 7–12 — primary right content (matches Mentors cards). */
export const gridRight = "md:col-start-7 md:col-span-6";

/** Columns 8–11 — inset right copy beside wide left media. */
export const gridRightMid = "md:col-start-8 md:col-span-4";

/** Columns 2–11 — wide copy blocks. */
export const gridWide = "md:col-start-2 md:col-span-10";

/** Columns 3–10 — centered headlines. */
export const gridCenter8 = "md:col-start-3 md:col-span-8";

/** Full 12-column span. */
export const gridSpan12 = "md:col-span-12";
