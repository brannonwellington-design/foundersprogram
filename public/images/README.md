# Image assets

Drop the Figma exports here using the exact filenames referenced in
`src/lib/content.ts`. Until a file is present, the UI shows an on-brand
placeholder and swaps to the real image automatically once added.

See the root `README.md` for the full list.
